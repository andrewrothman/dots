#!/bin/sh

# Encrypt stdin using a public key
# Usage: rsa-enc.sh <public_key.pem>

if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <public_key.pem>" >&2
    exit 1
fi

PUBKEY=$1

openssl pkeyutl -encrypt -pubin -inkey "$PUBKEY" | base64
